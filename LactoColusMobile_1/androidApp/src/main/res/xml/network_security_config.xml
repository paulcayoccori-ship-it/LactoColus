<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite trafico HTTP sin cifrar SOLO contra el backend Laravel en desarrollo:
  127.0.0.1 (dispositivo/host con `adb reverse`) y 10.0.2.2 (loopback del host
  visto desde el emulador Android). Cualquier otro dominio sigue exigiendo HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
